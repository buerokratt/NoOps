name: Dynamic Issue Title

on:
  issues:
    types: [opened]

jobs:
  update-title:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/github-script@v6
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const issue_number = context.issue.number;
            const body = context.payload.issue.body;

            // Extract Client and Version from issue body (GitHub Issue Form markdown)
            const clientMatch = body.match(/\*\*Client:\*\*\s*(.*)/i);
            const versionMatch = body.match(/\*\*Version deployed:\*\*\s*(.*)/i);

            const client = clientMatch ? clientMatch[1].trim() : "UnknownClient";
            const version = versionMatch ? versionMatch[1].trim() : "v0.0.0";

            // Extract date field from form if available, else use today
            const dateMatch = body.match(/\*\*Delivery Date:\*\*\s*(.*)/i);
            const date = dateMatch ? dateMatch[1].trim() : new Date().toISOString().slice(0,10);

            const newTitle = `[Deployment] ${client} - ${version} - ${date}`;

            await github.rest.issues.update({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number,
              title: newTitle
            });
