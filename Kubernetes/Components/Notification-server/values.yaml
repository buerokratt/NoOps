imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

podAnnotations: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

domain: test.buerokratt.ee # Domain name

#ingress:
#  tlsConfigMapName: byk-tls-configmap

ingress:
  certIssuerName: letsencrypt-prod-issuer

release_name: "component-notification-node"  

# byk:
#   dsl:
#     image:
#       registry: "ghcr.io"
#       repository: "buerokratt/buerokratt-dsl"
#       tag: "pre-alpha-kube-1.13.64"
#   ruuter:
#     dsl:
#       path: "/script/"

images:
  scope:
    registry: "ghcr.io"
    repository: ""
    tag: ""

env:
  OPENSEARCH_PROTOCOL: "http"
  OPENSEARCH_HOST: "opensearch-node"
  OPENSEARCH_PORT: "9200"
  OPENSEARCH_USERNAME: "admin"
  OPENSEARCH_PASSWORD: "admin"
  PORT: "4040"
  REFRESH_INTERVAL: "1000"
  CORS_WHITELIST_ORIGINS: "http://byk-widget:3001,http://byk-backoffice-gui:3000,http://byk-ruuter-private:8080,http://byk-ruuter:8080"
