apiVersion: v1
kind: Service
metadata:
  name: {{ include "buerokratt-vault.fullname" . }}
  labels:
    {{- include "buerokratt-vault.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "buerokratt-vault.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
