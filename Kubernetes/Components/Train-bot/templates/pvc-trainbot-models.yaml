apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-trainbot-models
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: longhorn
  resources:
    requests:
      storage: 500Mi  
