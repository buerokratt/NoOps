imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""
replicas: {{ trainbot.replicas }}
podAnnotations: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

domain: {{ domain.name }} # Domain name

#ingress:
#  tlsConfigMapName: byk-tls-configmap

ingress:
  certIssuerName: {{ cert.Issuer.Name }}

release_name: "component-byk-train-bot"  


images:
  scope:
    registry: "registry.gitlab.ria.ee"
    repository: "registry.gitlab.ria.ee/byk/rasa-for-buerokratt"
    tag: "{{ component.bot.image.version }}" # Images (tags) are up to date, currently no changes needed

bot:
  dsl:
    repo: https://gitlab.ria.ee/BYK/Installation-Guides.git
    path: default-setup/chatbot-and-training/bot

resources:
  requests:
    memory: "4Gi"
    cpu: "500m"
  limits:
    memory: "4Gi"
    cpu: "500m"
