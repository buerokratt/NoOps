imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

podAnnotations: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

domain: {{ domain.name }} # Domain name

ingress:
  certIssuerName: letsencrypt-prod # Change this if your certIssuerName has set up different

release_name: "post-deploy-common-service"  

images:
  scope:
    image: opensearchproject/opensearch:1.3.16
  opensearch:
    backoffice:
      repo: https://gitlab.ria.ee/BYK/Buerokratt-Chatbot.git
      path: DSL/OpenSearch
    services:
      repo: https://gitlab.ria.ee/BYK/Service-Module.git
      path: DSL/OpenSearch
    training:
      repo: https://gitlab.ria.ee/BYK/Training-Module.git
      path: DSL/OpenSearch
    common:
      repo: https://gitlab.ria.ee/BYK/Common-Services.git
      path: DSL/OpenSearch

env:
  CLUSTER_NAME: "opensearch-cluster"
  NODE_NAME: "component-opensearch-node"
  DISCOVERY_TYPE: "single-node"
  BOOTSTRAP_MEMORY_LOCK: "true"
  OPENSEARCH_JAVA_OPTS: "-Xms1g -Xmx1g"
  DISABLE_INSTALL_DEMO_CONFIG: "true"
  DISABLE_SECURITY_PLUGIN: "true"
