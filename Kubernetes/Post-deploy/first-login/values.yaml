imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

podAnnotations: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

domain: {{ domain.name }} # Domain name

ingress:
  certIssuerName: letsencrypt-prod # Change this if your certIssuerName has set up different

release_name: "job-postdeploy-first-login"

user:
  login: "{{ first.user.login }}" # Use a test ID cde. In production replace with real ID code "Every login value must contain EE"
  first_name: "{{ first.user.firstname}}"
  last_name: "{{ first.user.lastname }}"
  id_code: "{{ first.user.idcode }}" # Use a test ID cde. In production replace with real ID code  "Every ID code value must contain EE"
  display_name: "{{ first.user.displayname }}"
  csa_email: "{{ first.user.email }}"

db:
  db_host: component-databases-users-db
  db_port: "5432"
  db_user: byk
  db_password: {{ usersDB.password }} # Change password accordingly