{{- if .Values.global.istio.enabled }}
apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: "{{ .Values.global.release_name }}"
spec:
  host: "{{ .Values.global.release_name }}"
  subsets:
  - name: "{{ .Values.global.release_name }}"
    labels:
      app: "{{ .Values.global.release_name }}"
---
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: "{{ .Values.global.release_name }}"
spec:
  hosts:
  - "{{ .Values.global.release_name }}"
  http:
  - name: allow
    match:
    - sourceLabels:
        app: component-byk-ruuter
    - sourceLabels:
        app: component-byk-ruuter-private
    route:
    - destination:
        host: "{{ .Values.global.release_name }}"
        subset: "{{ .Values.global.release_name }}"
  - name: reject
    route:
    - destination:
        host: "{{ .Values.global.release_name }}"
        subset: "{{ .Values.global.release_name }}"
    fault:
      abort:
        percentage:
          value: 100.0
        httpStatus: 403
{{- end }}
