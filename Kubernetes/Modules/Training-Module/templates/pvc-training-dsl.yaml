apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-{{ namespace }}-training-dsl
spec:
  storageClassName: dsl
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
