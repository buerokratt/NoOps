nameOverride: module-byk-analytics-gui
fullnameOverride: module-byk-analytics-gui
replicaCount: 1
 
workload:
  kind: Deployment
  selectorLabels: {app: module-byk-analytics-gui}
 
image:
  repository: ghcr.io
  name: buerokratt/analytics-module
  tag: "v3.3.2"
  pullPolicy: IfNotPresent
 
containerPorts:
  - {name: http, containerPort: 3001}

env: []
 
resources:
  requests: {cpu: 1m, memory: 10Mi}
  limits: {cpu: 10m, memory: 50Mi}
 
services:
  http:
    name: module-byk-analytics-gui
    ports:
      http: {port: 3001, targetPort: http, protocol: TCP}
 
ingress: {enabled: false}
istio: {enabled: false}
 
constantsIni: |
  NODE_ENV=development