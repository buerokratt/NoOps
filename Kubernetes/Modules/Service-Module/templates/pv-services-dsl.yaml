apiVersion: v1
kind: PersistentVolume
metadata:
  name: services-{{ namespace }}-dsl-pv
spec:
  storageClassName: dsl
  capacity:
    storage: 200Mi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /data
