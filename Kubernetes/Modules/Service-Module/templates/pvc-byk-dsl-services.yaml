apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: byk-dsl-services
spec:
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteMany

