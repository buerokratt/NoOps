imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

podAnnotations: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

domain: test.buerokratt.ee # Domain name
secretname: test.buerokratt.ee1904prod # Cert name value, change this as suited to environment

ingress:
  certIssuerName: letsencrypt-prod # Change this if your certIssuerName has set up different

release_name: "module-byk-widget"

images:
  gui:
    registry: "ghcr.io"
    repository: "buerokratt/chat-widget"
    tag: "pre-alpha-sec-1.6.3" # Images (tags) are up to date, currently no changes needed
  dsl:
    registry: "ghcr.io"
    repository: "buerokratt/buerokratt-dsl"
    tag: "pre-alpha-1.3.2"  # Images (tags) are up to date, currently no changes needed

config: 
  header_origin: "https://test.buerokratt.ee" # Change the domain name, leave subdomain and subpath same
